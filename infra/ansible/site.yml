---
- name: Harden all nodes
  hosts: k3s_cluster
  become: true
  roles:
    - hardening

- name: Install K3s server
  hosts: control_plane
  become: true
  roles:
    - k3s-server

- name: Install K3s agents
  hosts: workers
  become: true
  roles:
    - k3s-agent
